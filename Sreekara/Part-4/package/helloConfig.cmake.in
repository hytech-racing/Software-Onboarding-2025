@PACKAGE_INIT@

#set_and_check(hello_INCLUDE_DIRS "${PACKAGE_INCLUDE_INSTALL_DIR}")

#set_and_check(hello_LIBRARIES hello::hello_library)

include("${CMAKE_CURRENT_LIST_DIR}/helloTargets.cmake")

check_required_components(hello_library)