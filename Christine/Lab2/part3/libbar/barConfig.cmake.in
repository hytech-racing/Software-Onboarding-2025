# expanded by configure_package_config_file() function in our CMakeLists.txt
@PACKAGE_INIT@

# Include the targets file which was generated & installed earlier
# By including barTargets.cmake, the barConfig.cmake file ensures 
# that the targets foo and bar are made available to any project 
# that calls find_package(bar).

include("${CMAKE_CURRENT_LIST_DIR}/barTargets.cmake")

# Check required components (ensure this is defined and used correctly)
check_required_components(bar)




